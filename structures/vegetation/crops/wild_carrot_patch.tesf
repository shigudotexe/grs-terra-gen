num carrots = 4 + randomInt(6);

for (num i = 0; i < carrots; i = i + 1) {
    num x = randomInt(6) - 1;
    num z = randomInt(6) - 1;
    
    if (getBlock(x, -1, z) == "minecraft:grass_block") {
        block(x, 0, z, "minecraft:carrots[age=7]");
    }
}