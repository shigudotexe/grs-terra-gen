id: DIAMOND_TIER_1_MED
type: FEATURE

anchors:
  - &structure DIAMOND_ORE_TIER_1_MED
  - &densityThreshold 1/256 * ${features/deposits/distribution.yml:diamond_tier_1_med.averageCountPerChunk}
  - &salt $features/deposits/distribution.yml:diamond_tier_1_med.salt
  - &range $features/deposits/distribution.yml:diamond_tier_1_med.range
  - &standard-deviation (${features/deposits/distribution.yml:diamond_tier_1_med.range.max} - ${features/deposits/distribution.yml:diamond_tier_1_med.range.min}) / 6

distributor:
  type: SAMPLER
  sampler:
    type: POSITIVE_WHITE_NOISE
    salt: *salt
  threshold: *densityThreshold

locator:
  type: GAUSSIAN_RANDOM
  amount: 1
  height: *range
  standard-deviation: *standard-deviation

structures:
  distribution:
    type: CONSTANT
  structures: *structure

