id: IRON_TIER_2_MED
type: FEATURE

anchors:
  - &structure IRON_ORE_TIER_2_MED
  - &densityThreshold 1/256 * ${features/deposits/distribution.yml:iron_tier_2_med.averageCountPerChunk}
  - &salt $features/deposits/distribution.yml:iron_tier_2_med.salt
  - &range $features/deposits/distribution.yml:iron_tier_2_med.range
  - &standard-deviation (${features/deposits/distribution.yml:iron_tier_2_med.range.max} - ${features/deposits/distribution.yml:iron_tier_2_med.range.min}) / 6

distributor:
  type: SAMPLER
  sampler:
    type: POSITIVE_WHITE_NOISE
    salt: *salt
  threshold: *densityThreshold

locator:
  type: GAUSSIAN_RANDOM
  amount: 1
  height: *range
  standard-deviation: *standard-deviation

structures:
  distribution:
    type: CONSTANT
  structures: *structure

